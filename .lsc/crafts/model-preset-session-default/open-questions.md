# Open Questions

## model-preset-session-default plan - 2026-07-13

- [x] **스펙 문면 게이트 편차 + 강화 술어 최종 승인 (랜딩 전 하드 게이트, 단일 사인오프)** — 스펙 제약 2·AC4는 fresh 게이트를 `ctx.sessionManager.getEntries().length === 0`로 문면 고정했으나, 핀 16.4.0 소스 확인 결과 신규 세션도 `session_start` 이전에 부트 스탬프(`model_change`/`thinking_level_change`/`service_tier_change`, `sdk.ts:2796-2811`)가 기록되어 **문면 조건이 인증된 부팅에서 영구 거짓**이다. plan.md Step 3은 의도 보존 구현으로 **강화 술어**를 채택했다(DR Decision B; 아키텍트 라운드1 B1 반영): (i) 전 엔트리 타입 ∈ 부트 스탬프 allowlist **AND** (ii) `model_change role:"default"` 부재 **AND** (iii) **각 스탬프 타입 출현 ≤ 1회**. (iii)은 라운드1이 소스로 증명한 반례 — 사용자 모델 **사이클** 픽은 role 미지정 `model_change`(`agent-session.ts:9144-9145, 9175-9176`)로 부트 스탬프와 동형이라, 사이클 전환 후 무턴 종료 → `--continue` resume에서 role 검사만으로는 게이트가 참이 되어 복원된 사용자 선택을 클로버한다 — 를 폐쇄한다(신선 부팅은 각 타입 최대 1회 — `sdk.ts:2801-2811` 직선 if). 서브에이전트는 `session_init` 배제(F2). **공지된 잔여 위양성(degenerate 클래스):** 모델 미해석 부팅(부트 `model_change` 부재)+**단일 비-default-role `model_change`(사이클/temporary 계열 — `agent-session.ts:9144-9176`, `:8985-8988`)**+무턴 종료+resume은 신선 부팅과 구분 불가 — R2에 클래스로 감수 기록(retry-fallback 계열은 mid-turn 한정이라 비성립). — *사용자 ACK는 이 **강화된 최종 의미론 전체**(allowlist + role + 개수 상한 + 잔여 위양성 클래스 공지)를 한 번에 승인하는 단일 사인오프이며, 구현 착수는 막지 않되 **머지 전 필수**다. Step 0 프로브(사이클-무턴-resume 시나리오 포함 5종)가 최종 근거를 제공한다.*
- [x] **레거시 평면 맵 안의 `default:` 키 처리** — plan 기본안은 **C2**(아키텍트 라운드1 권고로 C1에서 전환): 문면 그대로 agents-only 파싱 + validate 단계 예약 키 가드(경고+drop, 구조형 안내 문구 — plan Step 2). 스펙 문면 일치 + 생태계 규범("default는 명시적 sibling 필드, 역할 맵 내부 예약 이름 금지" — research/SYNTHESIS.md §3) 정합이며, 팬텀 `lsc-default` 오라우팅은 가드가 동등 차단한다(현행 validate는 키 이름 무검사라 가드 없이는 실제 적용됨). DWIM(평면 `default`를 `PresetEntry.default`로 파스 승격, C1)을 원하면 Step 2의 가드 1블록을 Step 1의 승격 1블록으로 치환하는 국소 변경. — *어느 쪽이든 기존 사용자 파일(7개 에이전트 키만 존재)에는 행동 차이가 없고, 평면 파일에 `default`를 적는 신규 사용자의 실수 경험만 달라진다(C2: 경고 1줄+미적용 / C1: 그대로 동작).*
- [x] **SKIP_LABEL 확정 문구 (AC9)** — plan 제안: `"(skip — inherit session model / preset default)"` (`src/preset/command.ts:21`). 신규 `DEFAULT_SKIP_LABEL = "(skip — no session default)"`도 함께 확정 필요. — *UX 문구는 리뷰어/사용자 취향 결정 사항이라 코드 착수 전 합의가 저렴하다.*

**RESOLVED 2026-07-13:** 세 항목 모두 사용자 단일 사인오프로 승인 — [Spec Deviation] lsc_confirm=yes (강화 술어 + 잔여 위양성 클래스 공지 + C2 + SKIP_LABEL 문구). 랜딩 전 하드 게이트 해소.
