# Open Questions — provider-usage-status-bar

No blocking spec ambiguity remains (interview gate cleared at 4.15% < 5%; the
AC2×AC3×AC7 viewport contradiction is resolved by the **A12** decision — a
**row-bounded collapsed summary** (prompt provably visible) plus an **on-demand
expanded overlay** for full detail, toggled by a keyboard shortcut + slash command).
A12 supersedes A11 (unbounded vertical growth), which the architect/critic proved
(`tui.ts:2705-2764`) scrolls the prompt fully **off-screen**. The items below are
craft-time **verification gates** the plan defers to live probes, not unresolved
requirements. Each is tracked inside `plan.md` (§6 Step 5 gates, §8 pre-mortem, §9
e2e). Both are **BLOCKING** craft gates with an explicit escalation path.

## provider-usage-status-bar plan (iteration 3, A12-updated) — 2026-07-15
- [ ] **Gate 0 — runtime value-import + registration smoke:** does `dist/main.js` load in a real omp (Bun) session after adding `"@oh-my-pi/pi-ai": "16.4.0"` to `package.json` `dependencies` and `import { resolveUsedFraction } from "@oh-my-pi/pi-ai/usage"`, plus a no-op `pi.registerCommand`/`pi.registerShortcut`? — The plugin's **first** runtime omp value import (everything else is `import type`) and a newly-declared direct dependency (`pi-ai` was undeclared, `package.json:19-26`). Expected safe (Bun executes the `.ts` the export resolves to; `resolveUsedFraction` is a stateless pure helper, so spike.ts finding #3's singleton hazard doesn't apply), but verify because it is novel. Pin `pi-ai` to exactly the installed `16.4.0` (the `pi-coding-agent` transitive version) so the direct declaration can't drift from the compiled type surface. **Fail → escalate:** AC9 mandates the exported normalizer, so a local reimplementation is a spec deviation requiring user sign-off, not a silent fallback. (Deferred: pre-craft is read-only.)
- [ ] **Gate 1 — A12 collapsed-fits + interaction (supersedes the old reflow-vs-clip gate):** in a real omp session at normal height **and** a ~10-row terminal with a tall transcript — (a) is the **collapsed** bar row-bounded (`rows ≤ deriveMaxRows(terminal.rows)`) so the **prompt stays visible** (never scrolled off)? This is provable by construction (rows ≤ maxRows < terminal.rows, `tui.terminal.rows`), but the live probe **tunes `RESERVE_ROWS`/`MAX_BAR_ROWS`** so the bar is useful without crowding the prompt. (b) Does the **keyboard shortcut** fire and **NOT type into the prompt** (routed via `CustomEditor.setCustomKeyHandler`, V1), and does the **`/usage-bar` slash command** fire — each opening the **expanded overlay** (`ctx.ui.custom({overlay:true})`)? (c) Does the overlay show full per-account/per-window detail, scroll for tall content, and does `Esc`/close return to the collapsed bar **without corrupting the transcript/prompt** (alt-screen, V4)? (d) Is refresh observed on timer/`turn_end`/`session_switch`, with **no leftover overlay** after `session_switch`/`session_shutdown`? — No in-tree `belowEditor`/overlay plugin caller exists (trace P4, residual). May also tune `STALE_AFTER_MS`/`BAR_CELLS` based on the observed fit. **Fail (collapsed still clips, chord types into the prompt, overlay corrupts the transcript, or the overlay leaks across sessions) → escalate to the user:** the bounding/overlay contract is violated; reconsider the reserve budget or the overlay mechanism — do NOT silently revert to unbounded vertical growth (A11 was explicitly superseded by A12). (Deferred: pre-craft is read-only.)

_Superseded (iteration 2, A11): the "reflow-vs-clip" gate is obsolete — the plan no longer grows the below-editor widget unboundedly. A12 bounds the collapsed default (prompt provably visible) and moves full detail to a focused alt-screen overlay; the live question is now collapsed-fits + keybind/command/overlay behavior, not reflow-vs-clip. (Superseded iteration 1: the still-earlier "stay unclipped … prompt never shifted" entry is likewise obsolete.)_
