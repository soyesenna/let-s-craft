{
  "name": "lets-craft",
  "version": "0.1.0",
  "description": "lets-craft — omp Extension plugin for the pre-craft / craft / post-craft development pipeline",
  "type": "module",
  "private": true,
  "license": "MIT",
  "omp": {
    "extensions": [
      "./dist/main.js"
    ]
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "e2e": "npm run build && LSC_E2E=1 vitest run test/e2e-full-cycle.test.ts test/e2e-worktree.test.ts test/enforcement-rules.test.ts --no-file-parallelism --testTimeout=10200000 --hookTimeout=120000"
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "16.4.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
